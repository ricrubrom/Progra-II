ORG 1000H
DIVI DW 0003H
NUM DW 0009H

ORG 3000H
RESTO:  PUSH AX
        PUSH CX
        PUSHF
WHILE:  CMP DX, CX ;CX TIENE EL DIVISOR Y DX EL NUMERO
        JS FIN
        SUB DX, CX
        JMP WHILE

FIN:    POPF
        POP CX
        POP AX
        RET

ORG 2000H
        MOV CX, DIVI
        MOV DX, NUM
        CALL RESTO
        HLT
END